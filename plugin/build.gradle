/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Gradle plugin project to get you started.
 * For more details take a look at the Writing Custom Plugins chapter in the Gradle
 * User Manual available at https://docs.gradle.org/7.1/userguide/custom_plugins.html
 */

plugins {
    // Apply the Java Gradle plugin development plugin to add support for developing Gradle plugins
    id 'java-gradle-plugin'
    // Apply the Groovy plugin to add support for Groovy
    id 'groovy'
    id 'maven-publish'
    id "com.gradle.plugin-publish" version "0.15.0"
}
repositories {
    // Use Maven Central for resolving dependencies.
    mavenCentral()
    mavenLocal()
}

dependencies {

}
group = 'com.vasug'
version = "0.0.1"

gradlePlugin {
    // Define the plugin
    plugins {
        taskTracker {
            id = 'net.asdfgh.tasktracker'
            displayName = 'TASK TRACKER'
            description = 'TASK TRACKER PLUGIN WHICH DISPLAYS TASK GRAPH AND TIME TAKEN BY TASKS'
            implementationClass = 'com.vasug.BuildTrackerPlugin'
        }
    }
}
publishing {
    repositories {
        mavenLocal()
        maven {
            mavenLocal()
        }
    }
}

pluginBundle {
    website = 'https://www.github.com/vasugr/'
    vcsUrl = 'https://www.github.com/vasugr'
    description = 'TASK TRACKER PLUGIN WHICH DISPLAYS TASK GRAPH AND TIME TAKEN BY TASKS'
    tags = ['plugin','gradle','groovy']

    plugins {
        taskTracker {
            // id is captured from java-gradle-plugin configuration
            displayName = 'Task Tracker plugin'
        }
    }
}